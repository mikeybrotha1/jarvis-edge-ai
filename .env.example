# Required for jarvis_ai and storage scripts
JARVIS_DATABASE_URL=postgresql://jarvis_app:replace_me@127.0.0.1:5432/jarvis_vision

# Optional: explicit YAML config path (must exist if set)
# JARVIS_CONFIG_PATH=config/jarvis.yaml

# Optional overrides (see docs/configuration.md)
# JARVIS_CAMERA_DEVICE=0
# JARVIS_LOG_LEVEL=INFO
